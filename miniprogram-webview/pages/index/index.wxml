<view class="page">
  <view class="card">
    <view class="title">诗扶小筑</view>
    <view wx:if="{{loading}}" class="desc">正在连接社区，请稍候...</view>
    <view wx:else class="desc error">{{error}}</view>
    <button wx:if="{{!loading}}" class="retry" bindtap="retry">重试</button>
  </view>
</view>
