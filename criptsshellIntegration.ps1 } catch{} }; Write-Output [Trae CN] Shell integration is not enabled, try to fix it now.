warning: in the working copy of 'src/server/routes/auth.routes.ts', LF will be replaced by CRLF the next time Git touches it
[1mdiff --git a/src/server/routes/auth.routes.ts b/src/server/routes/auth.routes.ts[m
[1mindex bdfcaf0..c025723 100644[m
[1m--- a/src/server/routes/auth.routes.ts[m
[1m+++ b/src/server/routes/auth.routes.ts[m
[36m@@ -54,11 +54,14 @@[m [mrouter.post('/register', async (req, res) => {[m
     const normalizedEmail = email.toLowerCase().trim();[m
     const name = (displayName || normalizedEmail.split('@')[0] || '匿名用户').trim();[m
 [m
[32m+[m[32m    console.log('[Auth] Register attempt:', { email: normalizedEmail, name });[m
[32m+[m
     const existing = await prisma.user.findUnique({[m
       where: { email: normalizedEmail },[m
     });[m
 [m
     if (existing) {[m
[32m+[m[32m      console.log('[Auth] Register failed - email already exists:', normalizedEmail);[m
       res.status(409).json({ error: '该邮箱已注册' });[m
       return;[m
     }[m
[36m@@ -66,6 +69,8 @@[m [mrouter.post('/register', async (req, res) => {[m
     const passwordHash = await bcrypt.hash(password, 12);[m
     const role = SUPER_ADMIN_EMAIL && normalizedEmail === SUPER_ADMIN_EMAIL ? PrismaUserRole.super_admin : PrismaUserRole.user;[m
 [m
[32m+[m[32m    console.log('[Auth] Creating user:', { email: normalizedEmail, role });[m
[32m+[m
     const user = await prisma.user.create({[m
       data: {[m
         email: normalizedEmail,[m
[36m@@ -80,9 +85,14 @@[m [mrouter.post('/register', async (req, res) => {[m
     const token = createToken(apiUser);[m
     setAuthCookie(req, res, token);[m
 [m
[32m+[m[32m    console.log('[Auth] Register success:', { uid: user.uid, email: user.email });[m
     res.status(201).json({ user: apiUser });[m
   } catch (error) {[m
[31m-    console.error('Register error:', error);[m
[32m+[m[32m    console.error('[Auth] Register error:', {[m
[32m+[m[32m      message: error instanceof Error ? error.message : 'Unknown error',[m
[32m+[m[32m      stack: error instanceof Error ? error.stack : undefined,[m
[32m+[m[32m      body: req.body,[m
[32m+[m[32m    });[m
     res.status(500).json({ error: '注册失败，请稍后重试' });[m
   }[m
 });[m
[36m@@ -95,21 +105,27 @@[m [mrouter.post('/login', async (req, res) => {[m
     };[m
 [m
     if (!email || !password) {[m
[32m+[m[32m      console.log('[Auth] Login failed - missing credentials');[m
       res.status(400).json({ error: '邮箱和密码不能为空' });[m
       return;[m
     }[m
 [m
[32m+[m[32m    const normalizedEmail = email.toLowerCase().trim();[m
[32m+[m[32m    console.log('[Auth] Login attempt:', { email: normalizedEmail });[m
[32m+[m
     const user = await prisma.user.findUnique({[m
[31m-      where: { email: email.toLowerCase().trim() },[m
[32m+[m[32m      where: { email: normalizedEmail },[m
     });[m
 [m
     if (!user) {[m
[32m+[m[32m      console.log('[Auth] Login failed - user not found:', normalizedEmail);[m
       res.status(401).json({ error: '邮箱或密码错误' });[m
       return;[m
     }[m
 [m
     const validPassword = await bcrypt.compare(password, user.passwordHash);[m
     if (!validPassword) {[m
[32m+[m[32m      console.log('[Auth] Login failed - invalid password for:', normalizedEmail);[m
       res.status(401).json({ error: '邮箱或密码错误' });[m
       return;[m
     }[m
[36m@@ -118,9 +134,14 @@[m [mrouter.post('/login', async (req, res) => {[m
     const token = createToken(apiUser);[m
     setAuthCookie(req, res, token);[m
 [m
[32m+[m[32m    console.log('[Auth] Login success:', { uid: user.uid, email: user.email });[m
     res.json({ user: apiUser });[m
   } catch (error) {[m
[31m-    console.error('Login error:', error);[m
[32m+[m[32m    console.error('[Auth] Login error:', {[m
[32m+[m[32m      message: error instanceof Error ? error.message : 'Unknown error',[m
[32m+[m[32m      stack: error instanceof Error ? error.stack : undefined,[m
[32m+[m[32m      body: req.body,[m
[32m+[m[32m    });[m
     res.status(500).json({ error: '登录失败，请稍后重试' });[m
   }[m
 });[m
[36m@@ -132,12 +153,17 @@[m [mrouter.post('/wechat/login', async (req, res) => {[m
     const photoURLRaw = typeof req.body?.photoURL === 'string' ? req.body.photoURL.trim() : '';[m
 [m
     if (!code.trim()) {[m
[32m+[m[32m      console.log('[Auth] WeChat login failed - missing code');[m
       res.status(400).json({ error: 'code 不能为空' });[m
       return;[m
     }[m
 [m
[32m+[m[32m    console.log('[Auth] WeChat login attempt');[m
[32m+[m
     const { openId, unionId } = await exchangeWechatLoginCode(code);[m
 [m
[32m+[m[32m    console.log('[Auth] WeChat code exchanged:', { openId, hasUnionId: !!unionId });[m
[32m+[m
     let user = await prisma.user.findFirst({[m
       where: {[m
         OR: [[m
[36m@@ -153,6 +179,8 @@[m [mrouter.post('/wechat/login', async (req, res) => {[m
       const passwordHash = await bcrypt.hash(generatedPassword, 12);[m
       const fallbackName = displayNameRaw || `微信用户${openId.slice(-6)}`;[m
 [m
[32m+[m[32m      console.log('[Auth] Creating new WeChat user:', { generatedEmail, fallbackName });[m
[32m+[m
       user = await prisma.user.create({[m
         data: {[m
           email: generatedEmail,[m
[36m@@ -172,6 +200,7 @@[m [mrouter.post('/wechat/login', async (req, res) => {[m
         (!user.wechatUnionId && !!unionId);[m
 [m
       if (shouldUpdateProfile) {[m
[32m+[m[32m        console.log('[Auth] Updating WeChat user profile:', { uid: user.uid });[m
         user = await prisma.user.update({[m
           where: { uid: user.uid },[m
           data: {[m
[36m@@ -188,6 +217,8 @@[m [mrouter.post('/wechat/login', async (req, res) => {[m
     const token = createToken(apiUser);[m
     setAuthCookie(req, res, token);[m
 [m
[32m+[m[32m    console.log('[Auth] WeChat login success:', { uid: user.uid, openId });[m
[32m+[m
     res.json({[m
       user: apiUser,[m
       token,[m
[36m@@ -197,7 +228,11 @@[m [mrouter.post('/wechat/login', async (req, res) => {[m
       },[m
     });[m
   } catch (error) {[m
[31m-    console.error('WeChat login error:', error);[m
[32m+[m[32m    console.error('[Auth] WeChat login error:', {[m
[32m+[m[32m      message: error instanceof Error ? error.message : 'Unknown error',[m
[32m+[m[32m      stack: error instanceof Error ? error.stack : undefined,[m
[32m+[m[32m      body: req.body,[m
[32m+[m[32m    });[m
     res.status(500).json({ error: error instanceof Error ? error.message : '微信登录失败' });[m
   }[m
 });[m
